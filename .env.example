# ── Servidor ─────────────────────────────────────────────────────────────────
PORT=3001
HOST=0.0.0.0

# ── Frontend (exposto ao Vite via VITE_) ─────────────────────────────────────
VITE_API_URL=http://localhost:3001/api
VITE_SOCKET_URL=http://localhost:3001
VITE_BASE_PATH=/

# ── SQL Server ────────────────────────────────────────────────────────────────
DB_HOST=
DB_PORT=
DB_DATABASE=
DB_USERNAME=
DB_PASSWORD=
DB_ENCRYPT=false
DB_TRUST_CERT=true

# ── OpenAI (opcional — deixe em branco para desativar enriquecimento de IA) ───
OPENAI_API_KEY=
OPENAI_ASSISTANT_ID=
